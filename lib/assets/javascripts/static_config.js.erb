if (!window.Drop) {
  window.Drop = {};
}

Drop.config = {
  JSON_CONFIG_URL: <%= Yajl::Encoder.encode(Drop.settings[:json_config_url]) %>,
  PATH_PREFIX: <%= Yajl::Encoder.encode(Drop.settings[:path_prefix]) %>,

  SIGNIN_URL: <%= Yajl::Encoder.encode(Drop.settings[:signin_url]) %>,
  SIGNOUT_URL: <%= Yajl::Encoder.encode(Drop.settings[:signout_url]) %>,
  SIGNOUT_REDIRECT_URL: <%= Yajl::Encoder.encode(Drop.settings[:signout_redirect_url]) %>,

  ALERT_DISMISS_URL: <%= Yajl::Encoder.encode(Drop.settings[:alert_dismiss_url]) %>,
  SHORTNER_URL: <%= Yajl::Encoder.encode(Drop.settings[:shortner_url]) %>,

  POSTS_PER_PAGE: 20,
  POST_TYPES: {
    file: "https://tent.io/types/file/v0#"
  }
};
