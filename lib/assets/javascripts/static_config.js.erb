if (!window.Drop) {
  window.Drop = {};
}

Drop.config = {
  JSON_CONFIG_URL: <%= Yajl::Encoder.encode(ENV['JSON_CONFIG_URL'] || "#{ENV['URL']}/config") %>,
  PATH_PREFIX: <%= Yajl::Encoder.encode(ENV['PATH_PREFIX']) %>,

  SIGNIN_URL: <%= Yajl::Encoder.encode(ENV['SIGNIN_URL']) %>,
  SIGNOUT_URL: <%= Yajl::Encoder.encode(ENV['SIGNOUT_URL'] || "#{ENV['URL']}/auth/signout") %>,
  SIGNOUT_REDIRECT_URL: <%= Yajl::Encoder.encode(ENV['SIGNOUT_URL'] ? ENV['SIGNOUT_REDIRECT_URL'] : ENV['SIGNOUT_REDIRECT_URL'] || ENV['URL']) %>,
  RESET_PASSPHRASE_URL: <%= Yajl::Encoder.encode(ENV['RESET_PASSPHRASE_URL']) %>,

  ALERT_DISMISS_URL: <%= Yajl::Encoder.encode(ENV['ALERT_DISMISS_URL']) %>,
  SHORTENER_URL: <%= Yajl::Encoder.encode(ENV['SHORTENER_URL']) %>,
  CONTACTS_URL: <%= Yajl::Encoder.encode(ENV['CONTACTS_URL']) %>,
  SIGIL_API_ROOT: <%= Yajl::Encoder.encode(ENV['SIGIL_API_ROOT']) %>,

  POSTS_PER_PAGE: 20,
  POST_TYPES: {
    file: "https://tent.io/types/file/v0#"
  }
};
