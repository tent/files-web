if (!window.Drop)
  window.Drop = {}

Drop.config = {
  JSON_CONFIG_URL: <%= Yajl::Encoder.encode(Drop.settings[:json_config_url]) %>,
  PATH_PREFIX: <%= Yajl::Encoder.encode(Drop.settings[:path_prefix]) %>
}
