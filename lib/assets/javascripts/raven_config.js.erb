if (typeof Raven !== 'undefined') {
	Raven.config(<%= Yajl::Encoder.encode(ENV['SENTRY_URL']) %>, {
		whitelistUrls: [/<%= URI(ENV['URL']).host %>/],
		fetchContext: true,
		linesofContext: 3
	}).install();
}
